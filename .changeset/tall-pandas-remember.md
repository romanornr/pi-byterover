---
"pi-byterover": minor
---

Add Hermes-grade ByteRover memory behavior with auto-created global config for shared memory, configurable shared memory cwd, primary read/write plus extra read-only memory workspaces, read-only mode, safer and better-scoped recalled-context injection, a lightweight recall-quality gateway that suppresses irrelevant/no-knowledge memory, and bounded pre-compaction persistence.
